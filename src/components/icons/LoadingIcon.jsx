import { CircularProgress } from "@mui/material";
import React from "react";

const LoadingIcon = () => {
    return <CircularProgress style={{ color: "#0284C7" }} />;
};

export default LoadingIcon;
